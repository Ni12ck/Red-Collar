# Руководство по тестированию API с Postman

## Быстрый старт

1. **Импортируйте коллекцию** в Postman:
   - File → Import → Upload Files
   - Выберите `geo-points-api.postman_collection.json`

2. **Проверьте переменные**:
   - Перейдите в Variables в коллекции
   - Установите `base_url` = `http://localhost:8000`

3. **Начните тестирование**:
   - Выполните "Login - Get JWT Token"
   - Токены сохранятся автоматически
   - Тестируйте остальные запросы

## Структура коллекции

### 🔐 Authentication (Аутентификация)
- **Login - Get JWT Token**: Получение access и refresh токенов
- **Refresh Token**: Обновление access токена

### 📍 Points API (API точек)
- **Get All Points**: Получить все точки пользователя
- **Create Point - Red Square**: Создать точку (Красная площадь)
- **Create Point - Bolshoi Theatre**: Создать точку (Большой театр)
- **Create Point - Invalid Coordinates**: Тест валидации (неверные координаты)
- **Get Point by ID**: Получить конкретную точку
- **Update Point**: Обновить точку
- **Search Points by Radius - 1km**: Поиск точек в радиусе 1 км
- **Search Points by Radius - 5km**: Поиск точек в радиусе 5 км
- **Search Points - Invalid Parameters**: Тест обработки ошибок

### 💬 Messages API (API сообщений)
- **Get All Messages**: Получить все сообщения пользователя
- **Create Message for Point**: Создать сообщение для точки
- **Create Another Message**: Создать еще одно сообщение
- **Get Message by ID**: Получить конкретное сообщение
- **Search Messages by Radius**: Поиск сообщений в радиусе

### ⚠️ Error Testing (Тестирование ошибок)
- **Unauthorized Access**: Доступ без авторизации
- **Invalid JWT Token**: Неверный токен
- **Not Found - Invalid Point ID**: Несуществующая точка

## Переменные коллекции

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `base_url` | Базовый URL API | `http://localhost:8000` |
| `access_token` | JWT токен доступа | Автоматически устанавливается |
| `refresh_token` | JWT токен обновления | Автоматически устанавливается |
| `point_id` | ID созданной точки | Автоматически устанавливается |
| `message_id` | ID созданного сообщения | Автоматически устанавливается |

## Сценарии тестирования

### 1. Полная цепочка тестирования
1. **Login** → Получить токены
2. **Create Point** → Создать точку, сохранить ID
3. **Get All Points** → Проверить создание
4. **Update Point** → Обновить точку
5. **Create Message** → Создать сообщение для точки
6. **Search Points/Messages** → Протестировать поиск по радиусу

### 2. Тестирование поиска
1. Создайте несколько точек с разными координатами
2. Используйте разные радиусы поиска
3. Проверьте результаты поиска

### 3. Тестирование ошибок
1. Попробуйте запросы без авторизации
2. Используйте неверные токены
3. Запросите несуществующие ресурсы
4. Отправьте неверные данные

## Советы по использованию

### Автоматическое сохранение переменных
Коллекция автоматически сохраняет:
- Токены после успешного логина
- ID созданных точек и сообщений
- Это позволяет использовать их в последующих запросах

### Отладка
- Используйте вкладку "Tests" для просмотра результатов
- Проверяйте Console для логов выполнения
- Все ошибки логируются в консоль Postman

### Настройка для разных сред
- **Локальная разработка**: `base_url = http://localhost:8000`
- **Docker**: `base_url = http://localhost:8000`
- **Production**: Установите соответствующий URL

## Примеры координат для тестирования

```json
// Москва, Красная площадь
{
  "latitude": 55.7558,
  "longitude": 37.6176,
  "title": "Красная площадь"
}

// Москва, Большой театр
{
  "latitude": 55.7601,
  "longitude": 37.6186,
  "title": "Большой театр"
}

// Санкт-Петербург, Дворцовая площадь
{
  "latitude": 59.9398,
  "longitude": 30.3146,
  "title": "Дворцовая площадь"
}
```

## Распространенные проблемы

### 401 Unauthorized
- Убедитесь, что выполнен вход и токены сохранены
- Проверьте срок действия токена (1 час по умолчанию)

### 404 Not Found
- Проверьте правильность URL
- Убедитесь, что ресурсы существуют

### 400 Bad Request
- Проверьте формат данных в теле запроса
- Убедитесь в правильности координат (широта: -90 до 90, долгота: -180 до 180)

### CORS ошибки
- API работает на localhost:8000
- Убедитесь, что Postman может подключаться к этому порту
